'use strict';
// Elaris expansion: world generation and combat share the adventure's existing state.
const EXPANSION_VERSION=3;
const CITY_ROOMS=JSON.parse(JSON.stringify(rooms));
const ELEMENTS=['fire','water','earth','air'];
const COUNTERS={fire:'water',water:'air',earth:'fire',air:'earth'};
const ELEMENT_ICONS={fire:'♨',water:'≈',earth:'◆',air:'≋',ice:'❄',poison:'☠'};
const CRYSTALS={capacity:{name:'Capacity',text:'+1 deck size',icon:'▥'},assault:{name:'Assault',text:'+1 attack-card damage',icon:'⚔'},aegis:{name:'Aegis',text:'+1 defense-card Block',icon:'◇'}};
let generatedLayouts={},spawnCatalog={},patrolCatalog={},activeRegion='city';
const cityTexture=new Image(),natureTexture=new Image();
cityTexture.src='assets/environment/neon-city.webp';natureTexture.src='assets/environment/elaris-atlas.webp';
const hashSeed=s=>[...String(s)].reduce((h,c)=>Math.imul(h^c.charCodeAt(0),16777619)>>>0,2166136261);
function seeded(seed){let n=seed>>>0;return()=>{n+=0x6D2B79F5;let t=n;t=Math.imul(t^t>>>15,t|1);t^=t+Math.imul(t^t>>>7,t|61);return((t^t>>>14)>>>0)/4294967296}}
function deviceSlots(){return [1,6,12,20,30].filter(l=>state.playerLevel>=l).length}
function deviceBonus(type){return(state?.device?.slots||[]).slice(0,deviceSlots()).filter(id=>state.device.crystals.find(c=>c.uid===id)?.type===type).length}
function installCrystal(slot,id){if(state.battle?.phase==='fight')return toast('Configure the Aetherlink between encounters.');if(slot<0||slot>=deviceSlots())return;if(id&&!state.device.crystals.some(c=>c.uid===id))return;state.device.slots=state.device.slots.map(x=>x===id?null:x);state.device.slots[slot]=id||null;save();showDevice()}
function showDevice(){if(!state)return;const slots=Array.from({length:5},(_,i)=>{const crystal=state.device.crystals.find(c=>c.uid===state.device.slots[i]);return '<div class="panel"><h3>Socket '+(i+1)+'</h3>'+(i>=deviceSlots()?'<p>Unlocks at level '+[1,6,12,20,30][i]+'</p>':'<p>'+(crystal?CRYSTALS[crystal.type].icon+' '+CRYSTALS[crystal.type].name:'Empty socket')+'</p><select data-socket="'+i+'"><option value="">Empty</option>'+state.device.crystals.map(c=>'<option value="'+c.uid+'" '+(crystal?.uid===c.uid?'selected':'')+'>'+CRYSTALS[c.type].name+' · '+c.uid+'</option>').join('')+'</select>')+'</div>'}).join('');openMenu('<div class="eyebrow">WRIST DEVICE · AETHERLINK</div><h1>Aetherlink Gauntlet</h1><p>Rare resonant crystals amplify your card interface. Elite enemies have an 8% crystal-drop chance; ordinary enemies never drop device crystals.</p><div class="device-core">◈</div><p>'+deviceSlots()+'/5 sockets · +'+deviceBonus('capacity')+' deck capacity · +'+deviceBonus('assault')+' damage · +'+deviceBonus('aegis')+' Block</p><div class="sheet-grid">'+slots+'</div><p>Capacity: +1 deck size. Assault: +1 damage per attack card. Aegis: +1 Block per defense card. Each crystal occupies one socket and can be moved freely outside combat.</p><p class="muted">Removing Capacity can leave an oversized deck. Remove cards before your next encounter.</p><button id="deviceClose">Return</button>');document.querySelectorAll('[data-socket]').forEach(s=>s.onchange=()=>installCrystal(Number(s.dataset.socket),s.value));$('deviceClose').onclick=closeMenu}

TALENT_BRANCHES.surge.nodes.push(
 {id:'critical',name:'Precision Strike',icon:'✧',max:5,tier:1,req:['powerCore',1],desc:'+3% critical chance per rank. Critical attacks deal double damage.'},
 {id:'doublePower',name:'Overdrive Pulse',icon:'⚡',max:1,tier:3,req:['overcharge',3],desc:'Once per encounter: double the next attack card\'s damage.'});
TALENT_BRANCHES.resolve.nodes.push({id:'echo',name:'Echo Protocol',icon:'⟳',max:1,tier:3,req:['recovery',3],desc:'Once per encounter: play the next card twice for one energy payment.'});
function addCard(id,name,cost,kind,icon,base){defs[id]={name,cost,kind,icon,...(base.soulbound?{soulbound:true}:{}),tiers:[0,1,2,3].map(level=>({...base,...(base.damage?{damage:base.damage+level*2}:{}),...(base.block?{block:base.block+level*2}:{})}))}}
addCard('glacial','Glacial Covenant',1,'Soulbound Ice','❄',{damage:4,freeze:1,element:'ice',soulbound:true,exhaust:true});
addCard('cinder','Cinder Lance',1,'Fire Attack','♨',{damage:5,burn:2,element:'fire'});
addCard('venom','Venom Bloom',1,'Poison Attack','☠',{damage:3,poison:2,element:'earth'});
addCard('gale','Gale Cut',1,'Air Attack','≋',{damage:5,draw:1,element:'air'});
for(const element of ELEMENTS)addCard('counter_'+element,element[0].toUpperCase()+element.slice(1)+' Reversal',1,'Elemental Counter',ELEMENT_ICONS[element],{block:3,counter:element,exhaust:true});
const originalRules=rules;
rules=function(c){const d=stat(c);return originalRules(c)+(d.freeze?' Freeze: enemy skips its next attack.':'')+(d.burn?' Burn: 2 damage for 2 turns.':'')+(d.poison?' Poison: +2 stacking damage each turn.':'')+(d.counter?' Arm '+d.counter+' to counter '+Object.keys(COUNTERS).find(e=>COUNTERS[e]===d.counter)+'.':'')};
syncTalentVitals=function(heal=false){const old=state.maxHp,next=30+state.playerLevel-1+talentRank('vitality')*2;state.maxHp=next;state.hp=Math.min(next,state.hp+(heal?Math.max(0,next-old):0))};
gainXP=function(n){const gained=[];if(state.playerLevel>=30){state.xp=0;return gained}state.xp+=n;while(state.xp>=100&&state.playerLevel<30){state.xp-=100;state.playerLevel++;gained.push(state.playerLevel);syncTalentVitals(false);state.hp=Math.min(state.maxHp,state.hp+5)}if(state.playerLevel===30)state.xp=0;return gained};

function configureRegion(region){activeRegion=region;for(const key of Object.keys(rooms))delete rooms[key];if(region==='city'){Object.assign(rooms,JSON.parse(JSON.stringify(CITY_ROOMS)))}else{const names=['Dawnroot Landing','Sunpetal Plains','Orchid Rainforest','Jade River','Fernveil Grove','Wildheart Meadow','Moon Orchid Vale','Sapphire Falls','Canopy Cathedral','Amber Savannah','Mistfall Basin','Elderbloom Sanctuary'];for(let y=0;y<3;y++)for(let x=0;x<4;x++){const key=x+','+y,index=y*4+x,exits={};if(x)exits.w=(x-1)+','+y;if(x<3)exits.e=(x+1)+','+y;if(y)exits.n=x+','+(y-1);if(y<2)exits.s=x+','+(y+1);rooms[key]={name:names[index],exits,district:index%4,landmark:['FOREST','PLAINS','RAINFOREST','RIVERLAND'][index%4],cityIndex:index}}}generatedLayouts={};spawnCatalog={};patrolCatalog={}}
function roomLayout(key){if(generatedLayouts[key])return generatedLayouts[key];const rand=seeded(hashSeed(state.seed+activeRegion+key)),r=rooms[key],size=.65+rand()*.5,cx=400,cy=250,blobs=[{x:cx,y:cy,rx:175*size,ry:135*size}],paths=[];
 const lobes=2+Math.floor(rand()*4);for(let i=0;i<lobes;i++){const a=i*Math.PI*2/lobes+rand()*.6;blobs.push({x:cx+Math.cos(a)*125*size,y:cy+Math.sin(a)*85*size,rx:(75+rand()*75)*size,ry:(60+rand()*65)*size})}
 for(const dir of Object.keys(r.exits)){const endpoint={n:[400,0],s:[400,500],w:[0,250],e:[800,250]}[dir];paths.push({a:[cx,cy],b:endpoint,width:45})}
 // Preserve access to original relics and the portal, with a connected side passage.
 if(r.relic)paths.push({a:[cx,cy],b:[r.relic[3],r.relic[4]],width:35});
 const layout={blobs,paths,variant:Math.floor(rand()*4),seed:hashSeed(state.seed+key+activeRegion),size};generatedLayouts[key]=layout;return layout}
function segmentDistance(x,y,a,b){const dx=b[0]-a[0],dy=b[1]-a[1],t=Math.max(0,Math.min(1,((x-a[0])*dx+(y-a[1])*dy)/(dx*dx+dy*dy||1)));return Math.hypot(x-a[0]-t*dx,y-a[1]-t*dy)}
function walkable(key,x,y,margin=0){if(x<0||x>800||y<0||y>500)return false;const l=roomLayout(key);return l.blobs.some(b=>((x-b.x)/(b.rx-margin))**2+((y-b.y)/(b.ry-margin))**2<1)||l.paths.some(p=>segmentDistance(x,y,p.a,p.b)<p.width-margin)}
NeonCity.blocked=(key,x,y)=>!walkable(key,x,y,8);
function floorPath(ctx,l){ctx.beginPath();for(const b of l.blobs){ctx.moveTo(b.x+b.rx,b.y);ctx.ellipse(b.x,b.y,b.rx,b.ry,0,0,Math.PI*2);ctx.closePath()}for(const p of l.paths){const dx=p.b[0]-p.a[0],dy=p.b[1]-p.a[1],len=Math.hypot(dx,dy),ox=-dy/len*p.width,oy=dx/len*p.width;ctx.moveTo(p.a[0]-ox,p.a[1]-oy);ctx.lineTo(p.b[0]-ox,p.b[1]-oy);ctx.lineTo(p.b[0]+ox,p.b[1]+oy);ctx.lineTo(p.a[0]+ox,p.a[1]+oy);ctx.closePath()}}
function paintRegion(canvas,key){const ctx=canvas.getContext('2d'),l=roomLayout(key),nature=activeRegion==='elaris',texture=nature?natureTexture:cityTexture,rand=seeded(l.seed);ctx.fillStyle=nature?'#062b26':'#080e1c';ctx.fillRect(0,0,800,500);
 if(texture.complete&&texture.naturalWidth){const cols=2,rows=nature?2:3,v=nature?l.variant:rooms[key].district;ctx.globalAlpha=.3;ctx.drawImage(texture,(v%cols)*texture.width/cols,Math.floor(v/cols)*texture.height/rows,texture.width/cols,texture.height/rows,0,0,800,500);ctx.globalAlpha=1}
 ctx.save();floorPath(ctx,l);ctx.clip();ctx.fillStyle=nature?'#466948':'#243041';ctx.fillRect(0,0,800,500);if(texture.complete&&texture.naturalWidth){const cols=2,rows=nature?2:3,v=nature?l.variant:rooms[key].district;ctx.drawImage(texture,(v%cols)*texture.width/cols,Math.floor(v/cols)*texture.height/rows,texture.width/cols,texture.height/rows,0,0,800,500)}for(let i=0;i<180;i++){ctx.fillStyle=nature?(i%2?'#b3d88d55':'#f6cf8055'):'#7fd9d122';ctx.fillRect(rand()*800,rand()*500,2+rand()*4,2)}ctx.restore();
 // Dark exterior and bright rim make collision boundaries readable over textured art.
 for(let y=0;y<500;y+=8)for(let x=0;x<800;x+=8)if(!walkable(key,x+4,y+4)){ctx.fillStyle=nature?'#062b26b8':'#070e1bd9';ctx.fillRect(x,y,8,8);if(walkable(key,x+12,y+4)||walkable(key,x+4,y+12)){ctx.fillStyle=nature?'#73a76f':'#3d7186';ctx.fillRect(x,y,8,3)}}
}
NeonCity.render=function(world,key,r){world.innerHTML='';world.dataset.environment=activeRegion;const canvas=document.createElement('canvas');canvas.className='city-ground';canvas.width=800;canvas.height=500;world.append(canvas);paintRegion(canvas,key);const title=document.createElement('div');title.className='city-landmark';title.textContent=(activeRegion==='elaris'?'ELARIS · ':'NEON AFTERMATH · ')+r.name;world.append(title);for(const [dir,raw]of Object.entries(r.exits)){const to=typeof raw==='string'?raw:raw.to,node=document.createElement('div');node.className='city-exit '+dir;node.textContent=(typeof raw==='object'&&!hasRelic(raw.requires)?'LOCKED · ':'')+rooms[to].name;world.append(node)}if(activeRegion==='elaris'){for(let i=0;i<4;i++){const wildlife=document.createElement('span');wildlife.className='wildlife';wildlife.textContent=i%2?'🦋':'✧';wildlife.style.left=(160+i*135)+'px';wildlife.style.top=(140+(i%2)*190)+'px';wildlife.style.animationDelay=(-i*2)+'s';world.append(wildlife)}}};
for(const image of [cityTexture,natureTexture])image.onload=()=>{if(state&&!state.battle)renderWorld()};

function roomSpawns(key){const cache=activeRegion+':'+key;if(spawnCatalog[cache])return spawnCatalog[cache];const rand=seeded(hashSeed(state.seed+cache+'enemies')),r=rooms[key],safe=activeRegion==='city'?key==='1,1':key==='0,0',count=safe?0:2+Math.floor(rand()*2),list=[];for(let i=0;i<count;i++){const types=activeRegion==='city'?['emberling','thornling','burrower','shade','cryptWisp']:['thornling','cryptWisp','vineguard','shade'];const boss=i===0&&activeRegion==='city'&&r.enemy&&enemies[r.enemy[0]].boss,type=boss?r.enemy[0]:types[Math.floor(rand()*types.length)];let x=400,y=250;for(let attempt=0;attempt<200;attempt++){x=170+rand()*460;y=110+rand()*280;if(walkable(key,x,y,28)&&Math.hypot(x-400,y-280)>85&&list.every(s=>Math.hypot(s.x-x,s.y-y)>90))break}const elite=!boss&&rand()<.22,element=activeRegion==='elaris'?ELEMENTS[(i+Math.floor(rand()*4))%4]:null;list.push({uid:cache+':'+i,type,x,y,boss,elite,element})}spawnCatalog[cache]=list;return list}
function spawnAvailable(s){return s.boss?!state.bosses.includes(s.type):!(state.cooldowns[s.uid]>0)}
enemyAvailable=function(id){const s=roomSpawns(state.room).find(s=>s.uid===id||s.type===id);return s?spawnAvailable(s):false};
roomCleared=function(key){return roomSpawns(key).every(s=>!spawnAvailable(s))};
function patrolFor(s){return patrolCatalog[s.uid]||(patrolCatalog[s.uid]={x:s.x,y:s.y,angle:hashSeed(s.uid)%628/100,turn:2})}
function chestFor(key){const designated=activeRegion==='city'?['-1,0','4,0','0,2','5,2']:['1,0','2,1','3,2'];if(!designated.includes(key))return null;const l=roomLayout(key),b=l.blobs[l.blobs.length-1];return{id:activeRegion+':'+key,x:b.x,y:b.y}}
renderWorld=function(){if(!state)return;const r=room(),world=$('world');if(collides(state.pos.x,state.pos.y))state.pos={x:400,y:280};NeonCity.render(world,state.room,r);for(const s of roomSpawns(state.room)){if(!spawnAvailable(s))continue;const p=patrolFor(s),node=document.createElement('div');node.className='enemy-node monster-'+s.type+(s.boss?' boss':'')+(s.elite?' elite':'');node.dataset.spawn=s.uid;node.dataset.name=(s.elite?'★ ELITE · ':'')+(s.element?ELEMENT_ICONS[s.element]+' ':'')+enemies[s.type].name;node.innerHTML=monsterArt(s.type);node.style.left=p.x+'px';node.style.top=p.y+'px';world.append(node)}if(r.relic&&relicAvailable(r.relic[0])){const q=r.relic,node=document.createElement('div');node.className='relic';node.textContent=q[2];node.dataset.name=q[1];node.style.left=q[3]+'px';node.style.top=q[4]+'px';world.append(node)}const chest=chestFor(state.room);if(chest&&!state.chests.includes(chest.id)){const node=document.createElement('div');node.className='secret-chest';node.textContent='▣';node.title='Weathered cache';node.style.left=chest.x+'px';node.style.top=chest.y+'px';world.append(node)}if((activeRegion==='city'&&state.room==='4,2'&&state.bosses.includes('thornWarden'))||(activeRegion==='elaris'&&state.room==='0,0')){const portal=document.createElement('button');portal.className='region-portal';portal.innerHTML='◉<small>'+(activeRegion==='city'?'Enter Elaris':'Return to city')+'</small>';portal.onclick=travelPortal;world.append(portal)}const player=document.createElement('div');player.id='player';player.style.left=state.pos.x+'px';player.style.top=state.pos.y+'px';player.innerHTML='<span class="hero-sprite"></span>';world.append(player);animateHero(0,false);renderHUD()};
animateEnemy=function(dt){if(!state||state.battle||!$('menuOverlay').classList.contains('hidden'))return;for(const s of roomSpawns(state.room)){if(!spawnAvailable(s))continue;const p=patrolFor(s),speed=s.boss?8:14;p.turn-=dt;if(p.turn<0){p.angle+=.8;p.turn=3}const nx=p.x+Math.cos(p.angle)*speed*dt,ny=p.y+Math.sin(p.angle)*speed*dt;if(walkable(state.room,nx,ny,26)&&Math.hypot(nx-s.x,ny-s.y)<75){p.x=nx;p.y=ny}else p.angle+=1.8;const node=document.querySelector('[data-spawn="'+s.uid+'"]');if(node){node.style.left=p.x+'px';node.style.top=p.y+'px';node.querySelector('.monster-sprite')?.style.setProperty('--enemy-facing',Math.cos(p.angle)<0?-1:1)}if(Math.hypot(state.pos.x-p.x,state.pos.y-p.y)<(s.boss?58:43)){startBattle(s.uid);if(state.battle)return}}};
checkWorldInteractions=function(){if(state.battle)return;const q=room().relic;if(q&&relicAvailable(q[0])&&Math.hypot(state.pos.x-q[3],state.pos.y-q[4])<48)collectRelic(q);const chest=chestFor(state.room);if(chest&&!state.chests.includes(chest.id)&&Math.hypot(state.pos.x-chest.x,state.pos.y-chest.y)<38){state.chests.push(chest.id);state.potions+=2;save();renderWorld();toast('Secret cache: two small healing potions!')}};
function travelPortal(){if(state.battle)return;if(activeRegion==='city'&&!state.bosses.includes('thornWarden'))return;const target=activeRegion==='city'?'elaris':'city';state.region=target;configureRegion(target);state.room=target==='elaris'?'0,0':'4,2';state.pos={x:400,y:300};state.visited=state.regionVisits[target]||[state.room];state.regionVisits[target]=state.visited;if(target==='elaris'&&!state.elarisGift){state.elarisGift=true;for(const element of ELEMENTS)state.pool.push(make('counter_'+element));toast('Elaris attunement: +4 deck capacity in this region and four counter cards. Open Deck to equip them.')}save();renderWorld()}

startBattle=function(spawnId){const issue=deckIssue();if(issue)return toast(issue+' Open Deck to adjust.');if(!active().length)return toast('Equip a card before fighting.');const spawn=roomSpawns(state.room).find(s=>s.uid===spawnId);if(!spawn||!spawnAvailable(spawn))return;const base=enemies[spawn.type],scale=spawn.elite?1.25:1,health=Math.round(base.hp*scale);keys={};state.fight++;state.battle={id:spawn.type,spawnId:spawn.uid,enemy:{...base,boss:spawn.boss,elite:spawn.elite,element:spawn.element,name:(spawn.elite?'Elite ':'')+base.name,hp:health,maxHp:health,attack:base.attack+(spawn.elite?2:0)},turn:1,energy:maxEnergy(),block:talentRank('plating'),draw:shuffle(active().map(c=>({...c}))),hand:[],discard:[],exhaust:[],savedUid:null,boostedUid:null,boostUses:0,debuffUsed:false,enemyDebuff:0,phase:'fight',logs:['Encounter: '+base.name],freeze:0,burn:0,burnTurns:0,poison:0,counter:null,doubleUsed:false,doubleArmed:false,echoUsed:false,echoArmed:false};drawCards(openingHand());$('battleOverlay').classList.remove('hidden');save();renderBattle()};
function cardEffect(c,empowered,doubleAttack){const b=state.battle,d=stat(c),boost=empowered?boostAmount():0;let damage=d.damage?d.damage+attackBonus()+deviceBonus('assault')+boost:0;if(damage&&doubleAttack)damage*=2;if(damage&&Math.random()<talentRank('critical')*.03){damage*=2;b.logs.push('Critical strike!')}if(damage&&b.enemy.element&&COUNTERS[b.enemy.element]===(d.element==='ice'?'water':d.element)){damage=Math.round(damage*1.5);b.logs.push('Elemental weakness: +50% damage.')}b.enemy.hp=Math.max(0,b.enemy.hp-damage);if(d.block)b.block+=d.block+deviceBonus('aegis')+boost;if(d.heal)state.hp=Math.min(state.maxHp,state.hp+d.heal+boost);b.energy+=d.energy||0;if(d.draw)drawCards(d.draw);if(d.freeze)b.freeze=Math.max(b.freeze,d.freeze);if(d.burn){b.burn=d.burn;b.burnTurns=2}if(d.poison)b.poison+=d.poison;if(d.counter){b.counter=d.counter;b.logs.push(d.counter+' counter armed.')}b.logs.push(d.name+(damage?' · '+damage+' damage':''))}
playCard=function(i){const b=state.battle;if(!b||b.phase!=='fight')return;const c=b.hand[i];if(!c)return;const d=stat(c);if(d.cost>b.energy)return;const boosted=hasTalent('powerCore')&&b.boostUses<boostCharges()&&b.boostedUid===c.uid,repeat=b.echoArmed&&hasTalent('echo')&&!b.echoUsed,overdrive=b.doubleArmed&&d.damage&&hasTalent('doublePower')&&!b.doubleUsed;b.energy-=d.cost;b.hand.splice(i,1);if(b.savedUid===c.uid)b.savedUid=null;if(boosted){b.boostUses++;b.boostedUid=null}if(repeat){b.echoUsed=true;b.echoArmed=false}if(overdrive){b.doubleUsed=true;b.doubleArmed=false}const original=owned(c.uid);if(original&&original.level<3)original.uses=Math.min(USES,original.uses+1);cardEffect(c,boosted,overdrive);if(repeat&&b.enemy.hp>0){b.logs.push('Echo Protocol repeats the card.');cardEffect(c,false,false)}(d.exhaust?b.exhaust:b.discard).push(c);if(!b.enemy.hp)winBattle();save();renderBattle()};
endTurn=function(){const b=state.battle;if(!b||b.phase!=='fight')return;const dot=b.poison+(b.burnTurns>0?b.burn:0);b.enemy.hp=Math.max(0,b.enemy.hp-dot);if(dot)b.logs.push('Burn / poison: '+dot+' damage.');if(b.burnTurns>0)b.burnTurns--;if(!b.enemy.hp){winBattle();save();renderBattle();return}if(b.freeze>0){b.freeze--;b.logs.push('Frozen: enemy skips its attack.')}else if(b.counter&&COUNTERS[b.enemy.element]===b.counter){b.enemy.hp=Math.max(0,b.enemy.hp-6);b.logs.push('Elemental counter: attack negated, 6 damage returned.');b.counter=null}else{const damage=Math.max(0,intent()-b.block);state.hp=Math.max(0,state.hp-damage);b.logs.push(b.enemy.name+' attacks for '+damage+' HP.');b.counter=null}b.block=0;const keep=hasTalent('retainCore')?b.hand.filter(c=>c.uid===b.savedUid).slice(0,1):[];b.discard.push(...b.hand.filter(c=>!keep.includes(c)));b.hand=keep;b.savedUid=null;if(!state.hp)loseBattle();else if(!b.enemy.hp)winBattle();else{b.turn++;b.energy=maxEnergy();drawCards(4)}save();renderBattle()};
winBattle=function(){const b=state.battle;if(b.phase!=='fight')return;b.phase='reward';state.wins++;const levels=gainXP(b.enemy.boss?50:b.enemy.elite?40:25);if(b.enemy.boss){if(!state.bosses.includes(b.id))state.bosses.push(b.id)}else state.cooldowns[b.spawnId]=3;b.potionDrop=Math.random()<.3;if(b.potionDrop)state.potions++;const rewardIds=activeRegion==='elaris'?['cinder','venom','gale',...ELEMENTS.map(e=>'counter_'+e)]:['cleave','riposte','bastion','mend','spark'];b.reward=make(rewardIds[Math.floor(Math.random()*rewardIds.length)]);state.pool.push(b.reward);if(state.deck.length<maxDeckSize()&&copiesInDeck(b.reward.id)<maxCopies())state.deck.push(b.reward.uid);b.special=[];if(b.id==='thornWarden'&&!state.iceGift){state.iceGift=true;const ice=make('glacial');state.pool.push(ice);b.special.push('Glacial Covenant: guaranteed Soulbound Ice card. The Elaris portal is now open!')}else if(b.enemy.boss&&Math.random()<.10){const soul=make(['phoenix','oath','verdict'][Math.floor(Math.random()*3)]);state.pool.push(soul);b.special.push('Soulbound boss card: '+stat(soul).name)}if(b.enemy.elite&&Math.random()<.08){const type=Object.keys(CRYSTALS)[Math.floor(Math.random()*3)],crystal={uid:'crystal-'+state.device.next++,type};state.device.crystals.push(crystal);b.special.push('Rare '+CRYSTALS[type].name+' crystal found! Slot it into the Aetherlink.')}b.levels=levels;b.logs.push('Victory! One random card added to your collection.')};
const baseLoseBattle=loseBattle;
loseBattle=function(){const b=state.battle;baseLoseBattle();state.room=activeRegion==='elaris'?'0,0':'1,1';state.pos={x:400,y:300};b.lastRegion=activeRegion;if(!active().length&&state.pool.length)state.deck=[state.pool[0].uid]};
const baseRenderBattle=renderBattle;
renderBattle=function(){const b=state.battle;if(!b)return;if(b.phase==='reward'){const m=$('battleModal');m.innerHTML='<div class="eyebrow">VICTORY · REWARDS COLLECTED</div><h2>'+stat(b.reward).name+'</h2><p>One random Impermanent card added to your collection.</p><div id="randomReward" class="cards"></div><p>'+(b.potionDrop?'Small healing potion found.':'No potion dropped.')+'</p>'+b.special.map(s=>'<p class="notice">'+s+'</p>').join('')+(b.levels.length?'<p>Level '+state.playerLevel+'! +'+b.levels.length+' maximum HP, +'+b.levels.length*5+' healing, and +'+b.levels.length+' talent point(s).</p>':'')+'<button id="continueReward" class="primary">Continue</button>';$('randomReward').append(cardElement(b.reward,()=>{},true));$('continueReward').onclick=finishBattle;return}baseRenderBattle();if(b.phase!=='fight')return;const tools=document.createElement('div');tools.className='combat-tools';tools.innerHTML='<p>Enemy element: <b>'+(b.enemy.element||'Neutral')+'</b>'+(b.enemy.element?' · Counter with '+COUNTERS[b.enemy.element]:'')+' · Freeze '+b.freeze+' · Burn '+b.burnTurns+' turns · Poison '+b.poison+(b.counter?' · Armed: '+b.counter:'')+'</p>'+(hasTalent('doublePower')?'<button id="overdrive" '+(b.doubleUsed?'disabled':'')+'>'+ (b.doubleUsed?'Overdrive spent':b.doubleArmed?'✓ Overdrive armed':'Overdrive · next attack ×2')+'</button>':'')+(hasTalent('echo')?'<button id="echoCard" '+(b.echoUsed?'disabled':'')+'>'+(b.echoUsed?'Echo spent':b.echoArmed?'✓ Echo armed':'Echo · play next card twice')+'</button>':'');$('battleModal').prepend(tools);if($('overdrive'))$('overdrive').onclick=()=>{b.doubleArmed=!b.doubleArmed;save();renderBattle()};if($('echoCard'))$('echoCard').onclick=()=>{b.echoArmed=!b.echoArmed;save();renderBattle()}};

newGame=function(){uid=1;const pool=['strike','strike','guard','guard','focus','mend'].map(id=>make(id));state={version:EXPANSION_VERSION,name:'Adventurer',characterId:'protagonist',seed:Math.floor(Math.random()*4294967295),region:'city',regionVisits:{city:['1,1'],elaris:['0,0']},playerLevel:1,xp:0,hp:30,maxHp:30,gold:0,potions:0,pool,deck:pool.map(c=>c.uid),room:'1,1',pos:{x:400,y:300},visited:['1,1'],relics:[],cleared:[],bosses:[],cooldowns:{},fight:0,wins:0,talents:{},battle:null,chests:[],device:{crystals:[],slots:[null,null,null,null,null],next:1}};state.visited=state.regionVisits.city;configureRegion('city');$('startOverlay').classList.add('hidden');$('menuOverlay').classList.add('hidden');$('battleOverlay').classList.add('hidden');setMaterials(0);save();renderWorld()};
save=function(){if(!state)return;state.regionVisits[state.region]=state.visited;localStorage.setItem('cardbound-expansion-v3',JSON.stringify(state))};
load=function(){try{const parsed=JSON.parse(localStorage.getItem('cardbound-expansion-v3'));if(!parsed||parsed.version!==3)throw Error('No save');const battle=parsed.battle;parsed.battle=null;validateImport(parsed);parsed.battle=battle;restoreGame(parsed);if(battle){$('battleOverlay').classList.remove('hidden');renderBattle()}return true}catch(e){toast('No valid expansion save found. Begin a new journey.');return false}};
function validateImport(s){if(!s||s.version!==3||!['city','elaris'].includes(s.region)||!Number.isInteger(s.seed)||s.seed<0||!Array.isArray(s.pool)||s.pool.length>10000||!Array.isArray(s.deck)||s.deck.length>30)throw Error('Unsupported or malformed save.');const ids=new Set();for(const c of s.pool){if(!c||!Object.hasOwn(defs,c.id)||!Number.isInteger(c.uid)||c.uid<1||ids.has(c.uid)||!Number.isInteger(c.level)||c.level<0||c.level>3||!Number.isInteger(c.uses)||c.uses<0||c.uses>50)throw Error('Invalid card data.');ids.add(c.uid);c.soulbound=!!defs[c.id].soulbound}if(new Set(s.deck).size!==s.deck.length||s.deck.some(id=>!ids.has(id)))throw Error('Invalid deck.');if(!Number.isInteger(s.playerLevel)||s.playerLevel<1||s.playerLevel>30||!Number.isFinite(s.hp)||s.hp<0||s.hp>1000||!Number.isFinite(s.maxHp)||s.maxHp<1||s.maxHp>1000||!Number.isFinite(s.xp)||s.xp<0||s.xp>=100||!Number.isInteger(s.potions)||s.potions<0)throw Error('Invalid player stats.');const validRooms=s.region==='city'?Object.keys(CITY_ROOMS):Array.from({length:12},(_,i)=>(i%4)+','+Math.floor(i/4));if(!validRooms.includes(s.room)||!s.pos||!Number.isFinite(s.pos.x)||!Number.isFinite(s.pos.y))throw Error('Invalid room.');for(const field of ['visited','relics','cleared','bosses','chests'])if(!Array.isArray(s[field])||s[field].some(x=>typeof x!=='string'||x.length>100||/[<>]/.test(x)))throw Error('Invalid world data.');if(!s.regionVisits||!Array.isArray(s.regionVisits.city)||!Array.isArray(s.regionVisits.elaris)||!s.cooldowns||!s.talents||!s.device||!Array.isArray(s.device.crystals)||s.device.crystals.length>1000||!Array.isArray(s.device.slots)||s.device.slots.length!==5)throw Error('Invalid progression data.');for(const [id,rank] of Object.entries(s.talents)){const node=Object.values(TALENT_BRANCHES).flatMap(b=>b.nodes).find(n=>n.id===id);if(!node||!Number.isInteger(rank)||rank<0||rank>node.max)throw Error('Invalid talent.')}const crystalIds=new Set();for(const c of s.device.crystals){if(!c||!Object.hasOwn(CRYSTALS,c.type)||typeof c.uid!=='string'||!/^crystal-\d+$/.test(c.uid)||crystalIds.has(c.uid))throw Error('Invalid crystal.');crystalIds.add(c.uid)}if(s.device.slots.some(id=>id!==null&&!crystalIds.has(id))||new Set(s.device.slots.filter(Boolean)).size!==s.device.slots.filter(Boolean).length)throw Error('Invalid sockets.');if(s.battle)throw Error('Save transfer is available between encounters only.');s.name='Adventurer';return s}
function restoreGame(s){state=s;uid=Math.max(1,...s.pool.map(c=>c.uid+1));configureRegion(state.region);state.regionVisits[state.region]=state.visited;keys={};$('startOverlay').classList.add('hidden');$('battleOverlay').classList.add('hidden');$('menuOverlay').classList.add('hidden');save();renderWorld()}
function exportSave(){if(!state)return toast('Begin or load a journey first.');if(state.battle)return toast('Finish the encounter before exporting.');const payload={format:'cardbound-save',version:3,state,materials:materials()},blob=new Blob([JSON.stringify(payload,null,2)],{type:'application/json'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='cardbound-'+state.region+'-level-'+state.playerLevel+'.json';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);toast('Save exported.')}
async function importSave(file){try{if(!file||file.size>2000000)throw Error('Choose a save file smaller than 2 MB.');const payload=JSON.parse(await file.text());if(payload.format!=='cardbound-save'||payload.version!==3)throw Error('Not a Cardbound expansion save.');const s=validateImport(payload.state);if(!Number.isInteger(payload.materials)||payload.materials<0||payload.materials>100000)throw Error('Invalid material count.');if(state&&!confirm('Replace this journey with the imported save?'))return;restoreGame(s);setMaterials(payload.materials);save();renderWorld();toast('Save imported successfully.')}catch(e){toast(e.message||'Unable to import this save.')}}
function showMainMenu(){keys={};openMenu('<div class="eyebrow">CARDBOUND</div><h2>Main menu</h2><p>Save transfer is available between encounters.</p><button id="exportSave">Export save file</button> <button id="importSave">Import save file</button><p><button id="backToGame">Return to game</button> <button id="restartJourney" class="danger">New journey</button></p>');$('exportSave').onclick=exportSave;$('importSave').onclick=()=>$('saveFile').click();$('backToGame').onclick=closeMenu;$('restartJourney').onclick=()=>{if(confirm('Start a new journey? Export your current save first if you want to keep it.'))newGame()}}
const baseShowCharacter=showCharacter;
showCharacter=function(){baseShowCharacter();const portrait=document.querySelector('.sheet-portrait');if(portrait){portrait.src='assets/characters/character-portal.webp';portrait.style.maxHeight='none';portrait.style.aspectRatio='16 / 9';portrait.style.objectFit='cover'}const info=document.createElement('p');info.className='notice';info.textContent='Aetherlink: '+deviceSlots()+' sockets · Critical strike '+talentRank('critical')*3+'% · Every level grants +1 max HP and heals 5 HP.';$('menuModal').append(info)};
objective=function(){if(activeRegion==='elaris')return 'Explore Elaris. Counter fire with water, water with air, earth with fire, and air with earth.';if(state.bosses.includes('thornWarden'))return 'The Warden portal is open. Return to Warden Mainframe to enter Elaris.';if(!hasRelic('ember'))return 'Search west for the Ember Sigil.';if(!hasRelic('boots'))return 'Clear Cable Market to recover the Briarstep Boots.';if(!hasRelic('lens'))return 'Find the Moon Lens in the Drowned Archive.';return 'Defeat the Thorn Warden to unlock Ice and Elaris.'};
showMap=function(){const keys=Object.keys(rooms),xs=keys.map(k=>Number(k.split(',')[0])),ys=keys.map(k=>Number(k.split(',')[1])),minX=Math.min(...xs),minY=Math.min(...ys),maxX=Math.max(...xs),maxY=Math.max(...ys);openMenu('<div class="eyebrow">'+(activeRegion==='city'?'NEON AFTERMATH':'ELARIS')+'</div><h2>Exploration map</h2><div class="region-map" style="grid-template-columns:repeat('+(maxX-minX+1)+',1fr)">'+keys.map(k=>{const [x,y]=k.split(',').map(Number),seen=state.visited.includes(k);return '<div class="panel '+(k===state.room?'current-room':'')+'" style="grid-column:'+(x-minX+1)+';grid-row:'+(y-minY+1)+'">'+(seen?rooms[k].name:'Unknown')+'<small>'+(k===state.room?'You are here':seen&&roomCleared(k)?'Clear':'')+'</small></div>'}).join('')+'</div><p>'+state.visited.length+'/'+keys.length+' areas visited. Connections: N / S / E / W as marked in each room.</p><button id="mapReturn">Return</button>');$('mapReturn').onclick=closeMenu};
const style=document.createElement('style');style.textContent='.device-core{font-size:90px;text-align:center;color:#78fff1;text-shadow:0 0 30px #41cdfc}.combat-tools{padding:12px;border:1px solid #529baf;margin-bottom:12px}.secret-chest{position:absolute;transform:translate(-50%,-50%);color:#d9b764;font-size:30px;text-shadow:0 0 8px #71dfaf;z-index:5}.region-portal{position:absolute;left:340px;top:340px;width:120px;height:95px;border:2px solid #9dffff;border-radius:50%;background:radial-gradient(#cdfff1,#215ca0,#22113d);box-shadow:0 0 35px #86e5e8;font-size:42px;z-index:7;animation:pulse 3s infinite}.region-portal small{display:block;font:12px system-ui}.elite .monster-sprite{filter:drop-shadow(0 0 8px #ffc958)}.region-map{display:grid;gap:5px}.region-map .panel{padding:8px;font:11px system-ui;min-height:60px}.region-map small{display:block;color:#9edcae}.current-room{outline:2px solid #fff189}.wildlife{position:absolute;pointer-events:none;z-index:3;color:#ceffee;font-size:19px;animation:wildlifeDrift 12s ease-in-out infinite}@keyframes wildlifeDrift{50%{transform:translate(50px,-20px)}}select{max-width:100%;background:#152e40;color:#e3faf7;padding:10px}';document.head.append(style);
const navDevice=document.createElement('button');navDevice.textContent='Aetherlink';navDevice.onclick=showDevice;$('nav').append(navDevice);const navMenu=document.createElement('button');navMenu.textContent='Menu';navMenu.onclick=showMainMenu;$('nav').append(navMenu);
const fileInput=document.createElement('input');fileInput.type='file';fileInput.accept='.json,application/json';fileInput.id='saveFile';fileInput.hidden=true;fileInput.onchange=()=>{importSave(fileInput.files[0]);fileInput.value=''};document.body.append(fileInput);
const importButton=document.createElement('button');importButton.textContent='Import save file';importButton.onclick=()=>fileInput.click();$('startOverlay').querySelector('section').append(importButton);
$('newBtn').onclick=newGame;$('resumeBtn').onclick=load;$('mapBtn').onclick=showMap;$('charBtn').onclick=showCharacter;
$('resumeBtn').classList.toggle('hidden',!localStorage.getItem('cardbound-expansion-v3'));
